[codespell]
# Skip generated/vendored files: lockfile integrity hashes and the bundled
# dist trip false positives. planText is a deliberate variable name.
skip = ./package-lock.json,./dist,./node_modules,./coverage,./badges,./.git
ignore-words-list = plantext
